{
  "name": "@pulse-hub/shared",
  "version": "1.0.0",
  "description": "Tipos y utilidades compartidas entre frontend y backend",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsc",
    "dev": "npx tsc --watch",
    "clean": "npx rimraf dist"
  },
  "dependencies": {
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  }
}

